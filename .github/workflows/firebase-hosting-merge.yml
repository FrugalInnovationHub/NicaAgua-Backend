# # This file was auto-generated by the Firebase CLI
# # https://github.com/firebase/firebase-tools

# name: Deploy to Firebase Hosting on merge
# 'on':
#   push:
#     branches:
#       - main
# jobs:
#   build_and_deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       # - uses: FirebaseExtended/action-hosting-deploy@v0
#       - run: firebase deploy
#         with:
#           repoToken: '${{ secrets.GITHUB_TOKEN }}'
#           firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_NICARAGUA_PROJECT }}'
#           channelId: live
#           projectId: nicaragua-project
#           target: nicaragua-api

# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy Firebase Functions
        run: |
          npm install -g firebase-tools
          firebase deploy --token ${{ secrets.FIREBASE_SERVICE_ACCOUNT_NICARAGUA_PROJECT }} --only functions
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_NICARAGUA_PROJECT }}

# name: Deploy to Firebase Hosting on merge

# on:
#   push:
#     branches:
#       - main

# jobs:
#   build_and_deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: '20'

#       - name: Install Firebase Tools
#         run: npm install -g firebase-tools

#       - name: Authenticate with Firebase
#         run: |
#           echo "$FIREBASE_SERVICE_ACCOUNT" > $HOME/gcp-key.json
#           firebase use --token "$FIREBASE_SERVICE_ACCOUNT"
#         env:
#           FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_NICARAGUA_PROJECT }}

#       - name: Deploy Firebase Functions
#         run: firebase deploy --only functions
